# Type opaque fort

- Peut être utilisé soit comme le type opaque, soit comme le type de base, mais nécessite un cast explicite vers le type de base
- Utile lorsqu'on veut empêcher que le type plus spécifique soit utilisé accidentellement dans des cas plus généraux, à moins de le caster explicitement
