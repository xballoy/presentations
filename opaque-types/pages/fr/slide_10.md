# Améliorer notre nouveau type

- Utiliser une clé de propriété calculée plutôt qu'un nom codé en dur
- Utiliser un `unique symbol` pour éviter la duplication
- Extraire l'utilitaire `Brand` dans son propre fichier pour empêcher l'accès à `__brand`
