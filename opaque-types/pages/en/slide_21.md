# Strong opaque type

- Can be used either as the opaque type or the base type, but needs to be explicitly cast to the base type
- Useful when we want to prevent the more specific type from being accidentally used in more general cases, unless we explicitly cast it
