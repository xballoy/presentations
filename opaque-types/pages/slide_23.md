# Super Opaque Type

- Can not be explicitly or implicitly cast to their base type
- Useful for cases where we are truly making a new type that is unrelated to any base type
- This is like comparing a `number` to a `string`
