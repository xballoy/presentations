# Strong Opaque Type

- Can be used either as the opaque type or the base type, but needs to be explicitly cast to the base type
- Useful when we don't want to accidentally use the more specific type for more general cases without a clear exception being made via explicit casting
